\chapter{Data}

\section{Data structure}
This thesis will cover a rather large (at least in the machine learning sense) climate data set. The data set covers two time periods; the "historical" period, spanning from 1951 till 2015, and the "future" data set, spanning from 2015 till 2100. In the data we find "images" of the climate, each image contains a number of \glspl{channel}, which covers one climatological variable each (e.g. temperature, precipitation, or wind). One "image" spans one day, and contains an aggregate of data from that day (either minimum, mean, or maximum). The dataset will cover two different climate models, a so-called \gls{gcm} and an \gls{rcm}. Each model covers two carbon emission scenarios (\gls{126} and \gls{370}), in the "future" time period. Additionally, each model has 3 \glspl{realisation} (ensembles) over the entire time period.

A visualisation  of the data structure can be found in \autoref{fig:data_structure}.

\begin{figure}[tb]
    \centering
    \includegraphics[width=0.8\linewidth]{figures/data_section/data_structure.png}
    \caption{Data structure of the \gls{ece} and \gls{hclim} data pair. It features two realisations for each of the two scenarios and the historical data. EC-Earth has a resolution of 380x180 pixels and 18 variables, while \gls{hclim} has 424x412 and 4 variables. It amounts to a total of 499.074.007.488 pixel-value climate estimates}
    \label{fig:data_structure}
\end{figure}

\subsection{Climate models} \label{sec:data-climate-model}
The machine learning task in this thesis is, of course \gls{downscaling}, which means that data must exist in two different resolutions. In this case, the two resolutions are populated by two different climate models. The low-resolution data is created by the \gls{ece} \gls{gcm}, while the high-resolution data is created by the \gls{hclim} \gls{ddm}. 

Naturally, the two climate models operate on two different grid projections. While the \gls{ece} data is on a regular latitude-longitude grid,the \gls{hclim} data uses a rotated polar grid. As the two projections map the Earth in wildly different manners, the \gls{ece} data set has to cover a much bigger area, than the \gls{hclim} data, to make sure that it encompasses all the of the \gls{hclim} area. The domains of the two datasets have been visualized in \autoref{fig:domains}.

\begin{figure}[tb]
    \centering
    \includegraphics[width=0.5\linewidth]{figures/data_section/domains.png}
    \caption{Spatial areas of the two climate model datasets. The blue line represents the outer edge of the \gls{ece} (\gls{gcm}) data, and the orange-red line represents the outer edge of the \gls{hclim} (\gls{ddm}) data}
    \label{fig:domains}
\end{figure}

In climate science, a climate model is a numerical estimator, which takes satellite data as input, as well as a set of hyperparameters, and uses an expensive physics-based method to calculate how the climate looks on three-dimensional grid (two spatial dimensions and one temporal). In other words, the models take the measured data and estimates how the corresponding climate look on the entire planet, while also estimating a number of years into the future. As the models are highly dependant on their initialisation it is customary to run several \glspl{realisation} to model the climate in a less biased fashion. To make sure, that such internal variation is accounted for, this data set includes 3 \glspl{realisation} for both the historical data, and both scenarios

\subsection{Scenarios} \label{sec:scenarios}
In the future period, for both data sets, there are two \glspl{scenario}; "\gls{126}" and "\gls{370}". SSP is short for "Shared Socioeconomic Pathways" and they describe different versions of how much emission is produced until the year 2100. \gls{126} is on the mild, green side of the spectrum, modelling a more sustainable future, where the excess energy on earth eventually declines as the human race start correcting their mistakes. \gls{370} is a more conservative estimate, in which the emission level will rise every year, rapidly heating up the planet.

\subsection{Static data}
Besides the two climate models, there are also two static features. In this sense "static" means that these features do not change over the temporal dimension. The two variables are "Orography", which describes the topography of the target area, and a "Land-Water mask" which describes the ratio of the grid point covered by water. Both features are delivered at a resolution matching the target grid.

\subsection{Variables}
As mentioned in \autoref{sec:scenarios} the \gls{ece} and \gls{hclim} data has 18 and 4 variables respectively. In this case there are two types of variables, single-level variables and pressure-level variables. Single-level variables are only recorded once and typically model surface level climate like 2 meter temperature or precipitation. Pressure-level variables are recorded on several levels of the atmosphere. In this data we are working with three different pressure levels 500, 700, and 850 hPa, and on each level we see variables like pressure, humidity, etc. See \autoref{tab:variables} for full list of variables. 

\begin{table}[bt]
    \centering
    \begin{tabular}{l|l|l|l}
        \multicolumn{3}{l}{\textbf{Single-level Variables}} \\ \toprule
        \underline{TAS}     & Temperature At Surface (2m)   & Kelvin                & 380x180 and 424x412 \\ \midrule
        \underline{TASMAX}  & Daily max TAS                 & Kelvin                & 380x180 and 424x412 \\ \midrule
        \underline{TASMIN}  & Daily min TAS                 & Kelvin                & 380x180 and 424x412 \\ \midrule
        \underline{PR}      & Precipitation                 & $\frac{kg}{m^2 s}$    & 380x180 and 424x412 \\ \midrule
        CLT                 & Cloud Area Fraction           & \%                    & 380x180 \\ \midrule
        PSL                 & Sea Level Pressure            & Pascal                & 380x180 \\ \midrule
        \multicolumn{3}{l}{\textbf{Pressure-level Variables} (500 hPa, 700 hPa, 850 hPa)} \\ \toprule
        UA                  & Eastward Wind                 & $m/s$                 & 380x180 \\ \midrule
        VA                  & Northward Wind                &  $m/s$                & 380x180 \\ \midrule
        HUS                 & Specific Humidity             & Watermass / Airmass   & 380x180 \\ \midrule
        TA                  & Air Temperature               & Kelvin                & 380x180 \\ \midrule
        \multicolumn{3}{l}{\textbf{Static variables}} \\ \toprule
        Orog                & Orography                     & m                     & 424x412 \\ \midrule
        SFTLF               & Sea Fraction To Land Fraction & \%                    & 424x412 \\ \midrule
    \end{tabular}
    \caption{Table of variables in dataset. Underlined variables are additionally used in as target data (albeit in a higher resolution).}
    \label{tab:variables}
\end{table}

\section{Data distribution} \label{sec:data-distribution}
In this section I will analyse the distributions and temporal developments of the data to develop an understanding of how the climate data can be modelled. This analysis is done prior to creating the model in order to best create preprocessing flows, that match the model capabilities and functions.

\subsection{Time-series data}
To start off let's have a look at the temporal development of the data. Specifically, let's model how the different \glspl{realisation} and \glspl{scenario} develop over time. This will help develop an understanding of the climate impact on temperature and precipitation through time. In the end, this will help us design the experiments and split data correctly. In \autoref{fig:tas-11-timeseries} we see such a visualisation.

\begin{figure}[tb]
    \centering
    \subbottom[11-year running mean of the yearly mean temperatures over the \gls{hclim} domain (see \autoref{fig:domains}), for \gls{scenario} SSP1-2.6.]{
        \includegraphics[width=.47\linewidth]{figures/data_section/climatology/timeseries_tas_11_ssp126.png}
    }
    ~
    \subbottom[11-year running mean of the yearly mean temperatures over the \gls{hclim} domain (see \autoref{fig:domains}), for \gls{scenario} SSP3-7.0.]{
        \includegraphics[width=.47\linewidth]{figures/data_section/climatology/timeseries_tas_11_ssp370.png}
    }
    \caption{11-year running mean of mean temperatures over Europe from 1951 to 2100. Colours blue, orange, and green represent realisation 1, 2, and 3 respectively. The vibrant lines show the \gls{370} scenario and the faded lines show the \gls{126} scenario. Dashed line shows data from the \gls{hclim} model, while solid lines show \gls{ece} data. The grey dashed line shows the end of the historical period (start of the future) in 2015.}
    \label{fig:tas-11-timeseries}
\end{figure}

One of the most noticeable features of \autoref{fig:tas-11-timeseries} is the early "historical" period. There are very large internal variabilities between the different \glspl{realisation}. Varying as much as 2.5 degrees. This is due to the different initialisation states (as mentioned in \autoref{sec:data-climate-model}). \Gls{realisation} 3 might be initialised during a period with El Niño and \gls{realisation} with La Niña. However, as we move towards the "future" (starting in 2015) the variability decreases. Around 2040, we see the effects of the two different \glspl{scenario} to kick in. The \gls{370} estimates accelerate in temperature rising a few degrees above that of the more climate-friendly \gls{126} estimate. We also notice a slight difference in temperatures between the low-resolution \gls{ece} and high-resolution \gls{hclim} data. The \gls{hclim} data initially has a slightly higher temperature than the \gls{ece}, however, as time progresses, the \gls{ece} overtakes and concludes at a temperature about 0.1 degrees higher.

Next, look at the development of the precipitation in the same time-frame. Since the distribution of the \glspl{realisation} through time will make the basis for designing the experiments it is important that conclusions made for the distribution of precipitation \glspl{realisation} are the same as those for temperature \glspl{realisation}.

\begin{figure}[tb]
    \centering
    \subbottom[11-year running mean of the yearly mean precipitation over the \gls{hclim} domain (see \autoref{fig:domains}), for \gls{scenario} SSP1-2.6.]{
        \includegraphics[width=.47\linewidth]{figures/data_section/climatology/timeseries_pr_11_ssp126.png}
    }
    ~
    \subbottom[11-year running mean of the yearly mean precipitation over the \gls{hclim} domain (see \autoref{fig:domains}), for \gls{scenario} SSP3-7.0.]{
        \includegraphics[width=.47\linewidth]{figures/data_section/climatology/timeseries_pr_11_ssp370.png}
    }
    \caption{11-year running mean of mean precipitation over Europe from 1951 to 2100. Colours blue, orange, and green represent \gls{realisation} 1, 2, and 3 respectively. The vibrant lines show the \gls{370} scenario and the faded lines show the \gls{126} scenario. Dashed line shows data from the \gls{hclim} model, while solid lines show \gls{ece} data. The grey dashed line shows the end of the historical period (start of the future) in 2015.}
    \label{fig:pr-11-timeseries}
\end{figure}

Looking at \autoref{fig:pr-11-timeseries} it is, again, apparent that the \glspl{realisation} have large internal variation in the "historical" period. Similarly \gls{realisation} 3 is in the higher end of the distribution, while \gls{realisation} 2 is on the lower end. On the other end, the difference in the \glspl{scenario} is not as apparent from this plot. For the \gls{ece} data the \gls{126} \glspl{realisation} has a lower mean precipitation than the \gls{370} \glspl{realisation}, however for the \gls{hclim} data, there is no notable difference. Comparing \gls{hclim} to \gls{ece} here shows a clear difference. There is more precipitation in \gls{hclim} data, for both \gls{126} and \gls{370}. The difference is about $10^{-6} \frac{kg}{m^{2} s}$ which is roughly 0.1 mm of precipitation per day. The difference most likely stems from the \gls{hclim} model's ability to model at a finer resolution and therefore captures more of the precipitation at mountain peaks and coastlines.

\section{Data splits}

 Now that the distribution of data has been analysed, it is possible to make some assumptions, which will help design data splits for the experiments. But before doing so, let's discus the purpose of the final model. It is important to design the test-split around what the model, in the end should compute. In this case, the purpose of the model is to downscale other \glspl{realisation} of the same \gls{gcm}-\gls{ddm} pair. Therefore, it is obvious to allocate entire \glspl{realisation} for testing. But how many, and which ones?

 In \autoref{sec:data-distribution} we argue that the first \gls{realisation} (r1i1p1f1) has the most centred distribution of data, in respect to "r2" and "r3". "r2" and "r3" are either on the lower or higher side. It is not know, whether "r1" is centred in the total population of \glspl{realisation}, however it cannot be an extremity. "r2" and "r3" on the other hand could potentially lie at the very edge of the distribution. As a rule of thumb, in machine learning, it is not ideal to train on extremities, as it will give the model an unrealistic idea of the total population. With these arguments it seems obvious to use \gls{realisation} 1 for training and \gls{realisation} 2 and 3 for testing.

 \section{Climatology}

